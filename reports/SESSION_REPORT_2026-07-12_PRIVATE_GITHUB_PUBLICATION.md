# Private GitHub publication report

Date: 2026-07-12  
Target: `obtuseai/nimrod`  
Visibility: private  
Status: `PRIVATE_REPOSITORY_PUBLICATION_AUTHORIZED`

## Publication scope

- Publish only the standalone lowercase `nimrod` project tree; exclude the unrelated umbrella workspace.
- Present the validated constitutional security architecture without claiming deployable or production-proven protection.
- Add a repository hero captured from the local evidence-first Crucible control board.
- Add a private GitHub proof workflow that runs the foundation, contract, integration, adversarial, recovery, and compile checks on Windows.
- Verify repository visibility, metadata, default branch, local/remote commit parity, and the terminal workflow result after push.

## Presentation evidence

- Desktop control-board capture: `docs/assets/nimrod-control-board.png`.
- Mobile check: 390 by 844 pixels with no horizontal overflow.
- Repository description: constitutional security control plane for governed AI, evidence-bound defense, authorized adversary validation, and verifiable recovery.

## Authority boundary

GitHub publication does not authorize candidate execution, production promotion, active-baseline mutation, offensive connector deployment, target access, credential acquisition, replication, persistence, compute expansion, or public distribution. The repository remains private and proprietary.

## Verification record

The exact remote commit, privacy result, workflow conclusion, and shared-copy digest are recorded in the external publication handoff after GitHub reaches a terminal state.

The first private-proof run exposed a Windows sharing race during concurrent signed board-ingress recovery. Durable ingress objects now publish through an atomic temporary-file hard link, and transient Windows read contention receives a short bounded retry. Persistent permission errors, malformed JSON, digest mismatch, replay, and conflicting ownership continue to fail closed.

The first fresh remote-clone audit also exposed platform-dependent newline emission in six generated validation reports. Validation writers now request canonical LF explicitly, preserving byte-for-byte v15 manifest reproducibility across Windows publication and fresh checkouts.

A later variance run exposed the recoverable record-to-head publication window directly. Inspection is now recovery-first and reports from the exact recovered record snapshot; a deterministic 50 ms widened-gap integration case and 30 repeated runs produced one winner and 15 replay denials per run.

Concurrent recovery may derive the same immutable record more than once. Atomic publication now distinguishes an already-existing path from corruption and accepts it only after exact digest equivalence; a conflicting record remains an integrity failure. Fifty widened-gap repetitions produced 50 winners and 750 replay denials across 800 attempts.
