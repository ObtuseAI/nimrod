# nimrod Edge foundation wave

## Session outcome

Status: `EDGE_FOUNDATION_REPLAYED_VERTICAL_SLICE_VALID_LIVE_ENFORCEMENT_BLOCKED`

This wave completed three bounded items: private GitHub governance hardening, Stage 0 decision formalization, and a runnable Windows-first Edge replay preview. The implementation remains an uncommitted review state on `codex/edge-foundation-wave`; no commit or push was requested.

## 1. Private GitHub governance

- The private `ObtuseAI/nimrod` repository now has an active default-branch ruleset with no bypass actors.
- Deletion and non-fast-forward updates are denied.
- Pull requests must resolve review threads and pass the strict `constitutional validation` status check.
- Vulnerability alerts, Dependabot security updates, secret scanning, and secret-scanning push protection are enabled.
- Paid Code Security remains disabled; this report does not claim that capability.

## 2. Stage 0 decision records

- Doctrine v0.1 is owner-approved through ADR-068 without claiming a cryptographic signature.
- The decision register now maps accepted Stage 0 identity, doctrine, Windows-first Edge, and private distribution decisions to evidence.
- Budget/accountable leads and design-partner recruitment remain open rather than being inferred.
- Approval creates no privileged installation, containment, offensive execution, source-staging, provisioning, distribution, or launch authority.

## 3. Runnable Edge vertical slice

- Accepts one deterministic offline Windows process-egress replay fixture.
- Applies a Budget 1 policy and emits a reversible `recommend_process_egress_isolation` proposal.
- Produces an E1 evidence receipt and four content-addressed Witness entries.
- Invokes an independent structural verifier in a separate Python process.
- Exposes the five-stage chain, uncertainty, explanation, authority, and Witness references in the Edge-first control-board UI.
- Contains no executor, privileged sensor, live endpoint mutation, network contact, approval service, or recovery action.

## Validation evidence

- Edge API flow: 1; Edge CLI flow: 1; independent verifier process: 1.
- Witness entries: 4; fail-closed adversarial cases: 7.
- Browser QA: HTTP 200, five visible stages, four Witness references, clickable proof console, zero console errors, and zero page errors.
- Contract ladder: 83 schemas, 83 positive examples, 83 negative mutations, 78 semantic contracts, and one migration.
- Execution authorized: false; execution performed: false; target state changed: false.
- Live endpoint observation, post-state proof, verified outcome, and recovery verification remain false.

## Shared review packet

The verified handoff target for this wave is `\\fileserver\shared\nimrod-review-packets\edge-foundation-wave-20260715`. The packet includes the review tree, UI screenshot, SHA-256 inventory, and copy proof. The local desktop shared-folder path was absent and was not used.

## Recommended next waves

1. Build the Windows supported-API feasibility spike and a read-only observation adapter that can produce real local evidence without containment authority.
2. Define the signed update, release provenance, rollback, and plugin-sandbox architecture before any privileged service exists.
3. Create the Edge design-partner interview, onboarding, privacy, and UX evidence kit; keep all Crucible execution blocked until an operator selects an isolated provider and independent observers.
